name: Continuous Deployment
on:
  workflow_call:
    secrets:
      hf_token:
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deployment To Hugging Face
        run: make deploy HF=${{ secrets.hf_token }}
